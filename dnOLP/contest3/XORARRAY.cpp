#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
  int n, q, a[100005];
  cin>>n>>q;
  for(int i=0; i<n; i++)
    cin>>a[i];
  while(q--){
    int l, x;
    cin>>l>>x;
    
  }
}

int main(){
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  solve();
  return 0;
}
